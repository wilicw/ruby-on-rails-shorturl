<%= form_with(model: makeurl, local: true) do |form| %>
  <% if makeurl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(makeurl.errors.count, "error") %> prohibited this makeurl from being saved:</h2>

      <ul>
      <% makeurl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :num %>
    <%= form.text_field :num, id: :makeurl_num %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :makeurl_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
